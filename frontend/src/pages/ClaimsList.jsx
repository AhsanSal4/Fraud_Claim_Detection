const ClaimsList = () => {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      {/* Empty page with only background */}
    </div>
  );
};

export default ClaimsList;
